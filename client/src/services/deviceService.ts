export async function updateDevice(device: string, value: boolean) {
    // await fetch('/api/device', {
    //     method: 'POST',
    //     headers: { 'Content-Type': 'application/json' },
    //     body: JSON.stringify({ device, value })
    // });
}